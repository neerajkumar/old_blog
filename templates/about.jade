extends layout

block prepend title
  | #{ page.title } -

block header

block content
  - var map = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December']
  - var article = new Object
  - _.chain(contents.about).map(function(item) {
  - article = item
  - })
  article.post.shadow

    section.content!= article.html

